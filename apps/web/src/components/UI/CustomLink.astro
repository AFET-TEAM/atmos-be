---
import Icon from "../../../public/icons/icon.astro";
import "./ui.scss";

type CustomLinkProps = {
  text: string;
  href: string;
  iconName: string;
  color?: string;
  size?: number;
  border?: boolean;
  className?: string;
  forceIconColor?: boolean
};

const {
  iconName,
  text,
  href,
  color = "#18a0fb",
  size = 24,
  className = "",
  border = false,
  forceIconColor = false,
} = Astro.props as CustomLinkProps;

const isActive =
  Astro.url.pathname === href ||
  (href !== "/" && Astro.url.pathname.startsWith(href + "/"));
---

{ text ? (
  <div class={`${className} customLink ${border ? "bordered" : ""} ${isActive ? "is-active" : ""}`}>
    <Icon name={iconName} size={size} stroke={isActive ? color : "#505050"} />
    <a href={href} class={`customLinks ${isActive ? "is-active" : ""}`} aria-current={isActive ? "page" : undefined}>
      {text}
    </a>
  </div>
) : (
  <a
    href={href}
    class={`${className} customLink ${border ? "bordered" : ""} ${isActive ? "is-active" : ""}`}
    aria-current={isActive ? "page" : undefined}
  >
    <Icon name={iconName} size={size} stroke={forceIconColor ? color : (isActive ? color : "#505050")} />
  </a>
) }
