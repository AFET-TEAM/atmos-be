---
import Icon from "public/icons/icon.astro";
import "./ui.scss";
type iconButton = {
  iconName: string;
  text?: string;
  color?: string;
  buttonType: string;
  onClick?: string;
};
const { iconName, text, color, buttonType, onClick } = Astro.props as iconButton;
---

<button class={`icon-button-${buttonType}`} onclick={onClick}>
  {text && <span>{text}</span>}
  <Icon color={color} name={iconName} size={24} />
</button>
